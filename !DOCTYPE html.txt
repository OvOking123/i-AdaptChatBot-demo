<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>AI Chatbot Demo for Business</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0; padding: 0;
      background: #f7f9fc;
      color: #333;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    header {
      background-color: #4A90E2;
      color: white;
      padding: 2rem;
      text-align: center;
    }
    header h1 {
      margin: 0 0 0.5rem 0;
    }
    main {
      flex: 1;
      max-width: 900px;
      margin: 2rem auto 4rem;
      padding: 0 1rem;
    }
    section {
      margin-bottom: 2rem;
    }
    h2 {
      color: #4A90E2;
      margin-bottom: 1rem;
    }
    p {
      line-height: 1.5;
    }
    #ai-chat-widget {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 320px;
      max-height: 420px;
      border: 1px solid #ccc;
      border-radius: 10px;
      background: white;
      box-shadow: 0 8px 24px rgba(0,0,0,0.15);
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      z-index: 9999;
    }
    #chat-header {
      background-color: #4A90E2;
      color: white;
      padding: 12px;
      font-weight: bold;
      text-align: center;
    }
    #chat-messages {
      flex: 1;
      padding: 10px;
      overflow-y: auto;
      font-size: 14px;
    }
    .chat-message {
      margin-bottom: 12px;
    }
    .chat-message.user {
      text-align: right;
    }
    .chat-message.user > div {
      background: #4A90E2;
      color: white;
      display: inline-block;
      padding: 8px 12px;
      border-radius: 15px 15px 0 15px;
    }
    .chat-message.bot > div {
      background: #e5e5ea;
      color: black;
      display: inline-block;
      padding: 8px 12px;
      border-radius: 15px 15px 15px 0;
    }
    #chat-input-container {
      padding: 10px;
      border-top: 1px solid #ddd;
      display: flex;
    }
    #chat-input {
      flex: 1;
      padding: 8px 12px;
      border: 1px solid #ccc;
      border-radius: 20px;
      font-size: 14px;
    }
    #chat-send-btn {
      margin-left: 8px;
      background-color: #4A90E2;
      color: white;
      border: none;
      border-radius: 20px;
      padding: 8px 16px;
      cursor: pointer;
    }
    #chat-send-btn:disabled {
      background-color: #a0bff6;
      cursor: not-allowed;
    }
    footer {
      text-align: center;
      padding: 1rem;
      color: #666;
      font-size: 0.9rem;
      border-top: 1px solid #ddd;
      margin-top: auto;
      background: #fafafa;
    }
  </style>
</head>
<body>
  <header>
    <h1>AI Chatbot Demo for Business Owners</h1>
    <p>See how AI can improve customer support and boost sales by answering questions instantly.</p>
  </header>
  <main>
    <section>
      <h2>About This Chatbot</h2>
      <p>This AI-powered chatbot is connected to an intelligent backend that adapts to your website's information and answers customer questions in real time.</p>
      <p>It's powered by OpenAI’s GPT API and runs through an n8n workflow that processes queries, ensuring personalized, accurate responses.</p>
    </section>
    <section>
      <h2>Try It Out!</h2>
      <p>Use the chat window in the bottom right corner to ask any question related to our demo business.</p>
      <p>Example questions:</p>
      <ul>
        <li>What are your business hours?</li>
        <li>Do you offer refunds?</li>
        <li>Can you tell me about your product warranty?</li>
      </ul>
    </section>
  </main>

  <!-- Chat Widget -->
  <div id="ai-chat-widget" aria-label="AI Chatbot">
    <div id="chat-header">Help Desk</div>
    <div id="chat-messages" role="log" aria-live="polite"></div>
    <form id="chat-form">
      <div id="chat-input-container">
        <input id="chat-input" type="text" placeholder="Ask me anything..." autocomplete="off" required />
        <button id="chat-send-btn" type="submit">Send</button>
      </div>
    </form>
  </div>

  <script>
    (() => {
      const widget = document.getElementById('ai-chat-widget');
      const messages = document.getElementById('chat-messages');
      const form = document.getElementById('chat-form');
      const input = document.getElementById('chat-input');
      const sendBtn = document.getElementById('chat-send-btn');

      // === REPLACE this with your actual n8n webhook URL ===
      const N8N_WEBHOOK_URL = 'https://YOUR_N8N_INSTANCE_URL/webhook/adaptive-ai-chatbot-http';

      // Customize your website context info here:
      const WEBSITE_CONTEXT = "Our demo company offers 24/7 support, 30-day returns, and a warranty on all products.";

      function appendMessage(text, sender = 'bot') {
        const msgDiv = document.createElement('div');
        msgDiv.classList.add('chat-message', sender);
        const innerDiv = document.createElement('div');
        innerDiv.textContent = text;
        msgDiv.appendChild(innerDiv);
        messages.appendChild(msgDiv);
        messages.scrollTop = messages.scrollHeight;
      }

      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const userText = input.value.trim();
        if (!userText) return;
        appendMessage(userText, 'user');
        input.value = '';
        sendBtn.disabled = true;

        try {
          const res = await fetch(N8N_WEBHOOK_URL, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({
              message: userText,
              websiteData: WEBSITE_CONTEXT
            })
          });

          if (!res.ok) throw new Error('Network error');

          const json = await res.json();
          if (json.answer) {
            appendMessage(json.answer, 'bot');
          } else {
            appendMessage('Sorry, I did not get a response.', 'bot');
          }
        } catch (err) {
          appendMessage('Error: ' + err.message, 'bot');
        } finally {
          sendBtn.disabled = false;
        }
      });
    })();
  </script>

  <footer>
    Demo site built with AI chatbot by [Your Name or Company]
  </footer>
</body>
</html>
